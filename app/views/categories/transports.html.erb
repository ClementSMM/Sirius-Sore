<h1>Transports</h1>
  
  <h2> <%= @category_sac.name  %> </h2>

  <% @category_sac.products.each do |produit| %>
    <%= link_to produit.name, product_path(produit) %>
    <%= produit.small_description  %>
  <% end %>
  
  <h2> <%= @category_pous.name  %> </h2>

  <% @category_pous.products.each do |produit| %>
    <%= link_to produit.name, product_path(produit) %>
    <%= produit.small_description  %>
  <% end %>
  
  <h2> <%= @category_sec.name  %> </h2>

  <% @category_sec.products.each do |produit| %>
    <%= link_to produit.name, product_path(produit) %>
    <%= produit.small_description  %>
  <% end %>