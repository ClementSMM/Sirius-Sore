<h1>Gamelles</h1>
  
  <h2> <%= @category_gam_s.name  %> </h2>

  <% @category_gam_s.products.each do |produit| %>
    <%= link_to produit.name, product_path(produit) %>
    <%= produit.small_description  %>
  <% end %>
  
  <h2> <%= @category_gam.name  %> </h2>

  <% @category_gam.products.each do |produit| %>
    <%= link_to produit.name, product_path(produit) %>
    <%= produit.small_description  %>
  <% end %>
  
  <h2> <%= @category_gam_g.name  %> </h2>

  <% @category_gam_g.products.each do |produit| %>
    <%= link_to produit.name, product_path(produit) %>
    <%= produit.small_description  %>
  <% end %>